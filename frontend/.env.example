# 通常无需设置：页面默认连接“当前访问主机”的 8765 端口。
# 只有后端位于其他主机或端口时才取消注释并修改。
# NEXT_PUBLIC_CRAWLER_API_BASE=http://127.0.0.1:8765/api/v1

# 后端在 .env.local 中设置了 CREDITCHINA_API_TOKEN 时，
# 前端必须提供相同的令牌才能调用接口。
# NEXT_PUBLIC_CRAWLER_API_TOKEN=
